import type { FC } from "react";
// import "./styles.scss";

type Props = {
  // picture: string;
};

export const ImageCard: FC<Props> = () => {
  return (
    <view>
      <text>Test</text>
    </view>
  );
};
